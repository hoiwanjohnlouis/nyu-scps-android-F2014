<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hoiwanlouis.mystockportfolio" >

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".PrototypeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:label="@string/main_activity_settings_title" >
        </activity>
        <activity android:name=".database.DatabaseAbstractActivity" />
        <activity android:name=".factories.TestFactoriesActivity" />
        <activity android:name=".observers.TestPriceObserver" />
        <activity
            android:name=".PrimoActivity"
            android:label="@string/title_activity_primo" >
        </activity>
    </application>

</manifest>
